# K2Edit Dependencies
# For development dependencies, use: pip install -e .[dev]
# For testing dependencies, use: pip install -e .[test]

# UI Framework
textual>=5.2.0
textual[syntax]>=0.51.0
rich>=13.3.3

# Core dependencies
aiofiles>=23.2.1
aiologger>=0.7.0
chardet>=5.0.0
# High-performance async event loop
uvloop>=0.19.0; sys_platform != "win32"

# AI and API integration
openai>=1.0.0
python-dotenv>=1.0.0

# Vector database and embeddings
chromadb>=1.0.15
# Minimal sentence-transformers setup - only additional core dependencies
# (tqdm already covered by rich dependency, numpy/scipy added as needed)
transformers==4.38.0
torch==2.7.1
numpy==2.3.2
scikit-learn
scipy
huggingface-hub>=0.19.3
sentence-transformers>=2.2.2

git+https://github.com/alaviss/tree-sitter-nim.git
